# Learned Rules

<!-- Max 50 lines. Each rule must have a `verify` line. -->
<!-- Rules without verify lines are wishes, not rules. -->

No rules have been promoted yet. Run `/evolve` after accumulating corrections.
